<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssr.newskuku.domain.news.mapper.NewsMapper">

    <!-- 전체 조회 -->
    <select id="findAll" resultType="com.ssr.newskuku.domain.news.dto.NewsResponse$FindAll">
        SELECT
            news_id as newsId,
            title,
            content,
            summary,
            url,
            thumbnail,
            category,
            created_at as createdAt,
            modified_at as modifiedAt,
            view_count as viewCount
        FROM news
        ORDER BY news_id DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>


    <!-- summary가 null인 뉴스 조회 -->
    <select id="findNewsWithoutSummary" resultType="com.ssr.newskuku.domain.news.News">
        SELECT
            news_id as newsId,
            title,
            content,
            summary,
            url,
            thumbnail,
            category,
            created_at as createdAt,
            modified_at as modifiedAt
        FROM news
        WHERE summary IS NULL
        ORDER BY news_id ASC
    </select>

    <!-- 상세 조회 -->
    <select id="findById" resultType="com.ssr.newskuku.domain.news.dto.NewsResponse$FindById">
        SELECT
            news_id as newsId,
            title,
            content,
            summary,
            url,
            thumbnail,
            category,
            view_count as viewCount,
            FORMATDATETIME(created_at, 'yyyy-MM-dd HH:mm') as createdAt,
            FORMATDATETIME(modified_at, 'yyyy-MM-dd HH:mm') as modifiedAt
        FROM news
        WHERE news_id = #{id}
    </select>

    <!-- 뉴스 저장 -->
    <insert id="save" parameterType="com.ssr.newskuku.domain.news.News">
        INSERT INTO news (title,content,url,category,thumbnail,is_write,published_at)
        VALUES (#{title},#{content},#{url},#{category},#{thumbnail},#{isWrite},#{publishedAt});
    </insert>

    <!-- URL 중복 체크 -->
    <select id="existsByUrl" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM news WHERE url = #{url}
    </select>

    <!-- AI 요약 업데이트 -->
    <update id="updateNewsSummary">
        UPDATE news
        SET summary = #{summary},
            modified_at = CURRENT_TIMESTAMP
        WHERE news_id = #{newsId}
    </update>

    <!-- 제목으로 검색 (페이징) -->
    <select id="findByKeyword" resultType="com.ssr.newskuku.domain.news.dto.NewsResponse$FindAll">
        SELECT
            news_id as newsId,
            title,
            content,
            summary,
            url,
            thumbnail,
            category,
            created_at as createdAt,
            modified_at as modifiedAt,
            view_count as viewCount
        FROM news
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY news_id DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 검색 결과 개수 -->
    <select id="countByKeyword" resultType="int">
        SELECT COUNT(*)
        FROM news
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <!-- 전체 뉴스 개수 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM news
    </select>

</mapper>