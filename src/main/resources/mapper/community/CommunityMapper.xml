<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssr.newskuku.domain.community.mapper.CommunityMapper">

    <!-- 1. 전체 게시글 조회 (페이징) -->
    <select id="selectAllCommunities" resultType="com.ssr.newskuku.domain.community.NewsCommunity">
        SELECT community_id, user_id, title, content, tag, img_url,
               view_count, created_at, updated_at
        FROM news_community
        ORDER BY created_at DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 2. 게시글 상세 조회 -->
    <select id="selectById" resultType="com.ssr.newskuku.domain.community.NewsCommunity">
        SELECT community_id, user_id, title, content, tag, img_url,
               view_count, created_at, updated_at
        FROM news_community
        WHERE community_id = #{communityId}
    </select>

    <!-- 3. 게시글 생성 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="communityId">
        INSERT INTO news_community (user_id, title, content, created_at, updated_at)
        VALUES (#{userId}, #{title}, #{content}, NOW(), NOW())
    </insert>

    <!-- 4. 게시글 수정 -->
    <update id="update">
        UPDATE news_community
        SET title = #{title},
            content = #{content},
            updated_at = NOW()
        WHERE community_id = #{communityId}
    </update>

    <!-- 5. 게시글 삭제 -->
    <delete id="delete">
        DELETE FROM news_community
        WHERE community_id = #{id}
    </delete>

    <!-- 6. 제목으로 검색 -->
    <select id="findByKeyword" resultType="com.ssr.newskuku.domain.community.NewsCommunity">
        SELECT community_id, user_id, title, content, tag, img_url,
               view_count, created_at, updated_at
        FROM news_community
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 7. 전체 게시글 개수 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM news_community
    </select>

    <!-- 8. 검색 결과 개수 -->
    <select id="countByKeyword" resultType="int">
        SELECT COUNT(*)
        FROM news_community
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
    </select>

</mapper>