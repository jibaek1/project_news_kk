<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssr.newskuku.domain.notification.mapper.NotificationMapper">

    <insert id="insert" parameterType="com.ssr.newskuku.domain.notification.Notification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO NOTIFICATION (USER_ID, MESSAGE, TYPE, IS_READ)
        VALUES (#{userId}, #{message}, #{type}, #{isRead})
    </insert>

    <select id="findByUser" parameterType="long" resultType="com.ssr.newskuku.domain.notification.Notification">
        SELECT ID, USER_ID, MESSAGE, TYPE, IS_READ,
        DATE_FORMAT(CREATED_AT, '%Y-%m-%d %H:%i') AS createdAt
        FROM NOTIFICATION
        WHERE USER_ID = #{userId}
        ORDER BY ID DESC
    </select>

    <select id="countUnread" parameterType="long" resultType="int">
        SELECT COUNT(*)
        FROM NOTIFICATION
        WHERE USER_ID = #{userId}
        AND IS_READ = 0
    </select>

    <update id="markAsRead">
        UPDATE NOTIFICATION
        SET IS_READ = 1
        WHERE ID = #{id}
        AND USER_ID = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM notification
        WHERE id = #{id}
        AND user_id = #{userId}
    </delete>

</mapper>